import { useEffect, useState } from "react"

export default function Users() {
    // declared an state
    const [users, setUsers] = useState([]);
    // to load data using useEffect function with dependency
    useEffect(() => {

    }, [])

    return (
        <div>

        </div>
    )
}